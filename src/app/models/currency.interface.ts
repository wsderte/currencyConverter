export interface ICurrency {
        exchangedate?: string,
        r030?: number,
        cc?: string,
        txt?: string,
        enname?: string,
        rate?: number,
        units?: number,
        rate_per_unit?: number,
        group?: string,
        calcdate?: string,
}